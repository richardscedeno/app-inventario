import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cedenorichards.appinventario',
  appName: 'appInventario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
